services:
  - type: web
    name: ai-sales-team-dashboard
    env: python
    rootDir: sales-ops-agent
    buildCommand: pip install -r requirements.txt
    startCommand: python3 -m app.web_server
    plan: starter
